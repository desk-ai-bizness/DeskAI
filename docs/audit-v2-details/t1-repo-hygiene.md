# Phase 0: Repository Hygiene Audit

**Auditor**: Agent T1
**Commit**: 75715f2
**Date**: 2026-04-03

## Findings

| # | Severity | File/Path | Issue | Recommendation | Pre-Inv Ref |
|---|----------|-----------|-------|----------------|-------------|
| 001 | HIGH | `.DS_Store` | macOS metadata file (6,148 bytes) is tracked in git despite `.gitignore:12` listing `.DS_Store`. It was committed in 75715f2. `.gitignore` only prevents future *untracked* files from being staged; once tracked, the pattern has no effect. | Run `git rm --cached .DS_Store` and commit. The existing `.gitignore` pattern will then prevent re-addition. | H1 |
| 002 | HIGH | `.poc-frontend/` | Directory contains two unreferenced Gemini-generated PNGs totaling 12,912,776 bytes (~12.3 MB) plus a 58 KB `index.html`. The PNGs (`Gemini_Generated_Image_lmtjp7lmtjp7lmtj.png`, `Gemini_Generated_Image_opr9tuopr9tuopr9.png`) are **not referenced** by `index.html` (0 grep matches) and not referenced anywhere else in the codebase (only hit is the audit prompt doc itself). The HTML is a standalone "Medic AI" landing page POC with all CSS inlined. All three files are tracked in git. `.prettierignore:9` lists `.poc-frontend` but `.gitignore` does not. | 1) `git rm -r --cached .poc-frontend/` to untrack. 2) Add `.poc-frontend/` to `.gitignore`. 3) If the POC has historical value, tag the commit before removal. 4) Consider `git filter-branch` or BFG to purge the 12 MB from history if repo size is a concern. | H2 |
| 003 | MEDIUM | `app/tsconfig.app.tsbuildinfo` | TypeScript incremental build artifact tracked in git. This is a machine-generated JSON file that changes on every build and causes noisy diffs. | `git rm --cached app/tsconfig.app.tsbuildinfo` and add `*.tsbuildinfo` to `.gitignore`. | H3 |
| 004 | MEDIUM | `app/tsconfig.node.tsbuildinfo` | Same issue as #003 — second TypeScript build info file tracked. | Same fix as #003 — the single `*.tsbuildinfo` glob covers both. | H4 |
| 005 | MEDIUM | `infra/deskai_infra.egg-info/` | Python egg-info directory (5 files: `PKG-INFO`, `SOURCES.txt`, `dependency_links.txt`, `requires.txt`, `top_level.txt`) committed. These are generated by `pip install -e .` and change on every install. | `git rm -r --cached infra/deskai_infra.egg-info/` and add `*.egg-info/` to `.gitignore`. | H5 |
| 006 | MEDIUM | `backend/src/deskai_backend.egg-info/` | Same egg-info issue in the backend package (5 files tracked). Not in the original pre-investigation list. | `git rm -r --cached backend/src/deskai_backend.egg-info/` — same `*.egg-info/` gitignore glob covers it. | NEW |
| 007 | MEDIUM | `.github/workflows/app-ci.yml` | No test step. The app CI pipeline runs `npm install` (line 23), `npm run lint` (line 25), `npm run typecheck` (line 27), and `npm run build` (line 29) but never runs tests. Furthermore, `app/package.json` has **zero test dependencies** (no vitest, jest, or any test runner) and no `test` script. This means the frontend has no automated test infrastructure at all. | Add a test framework (vitest recommended for Vite projects) and a `test` step in the workflow. For a medical SaaS, untested frontend is a risk. | H6 |
| 008 | MEDIUM | `.github/workflows/infra-ci.yml:4` | The `pull_request` trigger on line 4 has **no `paths` filter**, meaning `infra-ci.yml` runs on every PR to the repo regardless of what files changed. The `push` trigger (lines 5-8) correctly scopes to `infra/**`. The workflow partially mitigates this with a `dorny/paths-filter` step in the `detect-changes` job (lines 11-23) that gates the actual `infra` job, but this still spins up a runner for every PR just to run the filter check. | Add `paths: ['infra/**', '.github/workflows/infra-ci.yml']` to the `pull_request` trigger to avoid unnecessary runner allocation. The `detect-changes` job can remain as defense-in-depth. | H7 |
| 009 | LOW | `.github/workflows/deploy.yml` | Entire workflow is a placeholder. The single step on line 19 runs `echo "Deployment pipeline placeholder. Implement in Task 004+."`. If manually triggered via `workflow_dispatch`, it will succeed silently and deploy nothing, which could mislead operators. | Either (a) remove the file until a real pipeline exists, or (b) add a clear `exit 1` with an error message so accidental dispatch fails loudly: `echo "ERROR: Deploy not implemented" && exit 1`. | H8 |
| 010 | LOW | `.github/CODEOWNERS:2` | Single wildcard rule `* @gabrielmendes98 @Portoni @danielportoni @pedrohft` assigns all four owners to every file. No path-specific ownership for `backend/`, `infra/`, `app/`, `contracts/`, or `.github/workflows/`. In practice, all four owners are required reviewers for every PR, which defeats the purpose of CODEOWNERS for routing. | Add path-specific rules, e.g., `/backend/ @backend-owner`, `/infra/ @infra-owner`, `/app/ @frontend-owner`. Keep `*` as a fallback. | H9 |
| 011 | LOW | `.gitignore` | Missing patterns for build artifacts that are currently tracked: `*.egg-info/` (Python), `*.tsbuildinfo` (TypeScript). Also missing `.poc-frontend/` if that directory is to be ignored. The `.gitignore` also lacks `*.egg` and `*.whl` for Python wheel artifacts, and `.jsii-cache` (referenced in `infra/Makefile:21` synth target). | Add: `*.egg-info/`, `*.tsbuildinfo`, `.poc-frontend/`, `*.egg`, `*.whl`, `.jsii-cache`. | H10 |
| 012 | LOW | `.prettierignore:9` vs `.gitignore` | `.prettierignore` lists `.poc-frontend` (line 9), acknowledging the directory should be excluded from formatting. But `.gitignore` does not list it, so the files remain tracked. This inconsistency suggests `.poc-frontend` was meant to be ignored but the gitignore was never updated. | Add `.poc-frontend/` to `.gitignore` (see finding #002). | H11 |
| 013 | LOW | `Makefile` | Root Makefile defines `lint`, `format`, `format-check`, `backend-test`, `build-lambda`, and `infra-synth` but has **no `app-test` target**. The `backend-test` target delegates to `cd backend && make test`, but there is no equivalent for the frontend. This means `make` at the root cannot run a full test suite. | Add `app-test` target (once vitest is set up per #007). Consider an `all-test` or `check` target that runs all suites. | H12 |
| 014 | MEDIUM | CI pipelines | **No contract validation in CI.** The root `package.json:10` defines a `lint:contracts` script that runs `tools/lint-contracts.sh` (which calls `redocly lint` on `contracts/http/*.yaml` and `prettier --check contracts/**/*.yaml`). However, **none of the four CI workflows invoke this script**. Contract changes can be merged without validation. | Add a dedicated `contracts-ci.yml` workflow triggered on `contracts/**` changes, or add a contract lint step to an existing workflow. | NEW |
| 015 | MEDIUM | CI pipelines | **No security scanning.** No Dependabot config (`.github/dependabot.yml` missing), no CodeQL workflow, no SAST/DAST tooling, no secret scanning, no SECURITY.md policy file. The `.vscode/settings.json:2` has `"snyk.advanced.autoSelectOrganization": true`, suggesting Snyk is used locally but is not wired into CI. For a medical SaaS handling health data, this is a gap. | Add at minimum: Dependabot for dependency updates, CodeQL or Snyk for SAST in CI, and a `SECURITY.md` vulnerability disclosure policy. | NEW |
| 016 | LOW | `package-lock.json` (root) | 188 KB lockfile at repo root. This is **intentional** — the root `package.json` defines monorepo-level dev tooling (prettier, markdownlint-cli2, stylelint, redocly). The lockfile ensures deterministic installs for these tools. No action needed. | None — correct usage for monorepo tooling. | N/A |
| 017 | INFO | `.vscode/settings.json` | Tracked per `.gitignore:3` (`!.vscode/settings.json` exemption). Contains only `"snyk.advanced.autoSelectOrganization": true`. This is a team-shared IDE setting, which is fine, but it references a tool (Snyk) that has no CI integration (see #015). | Consider whether this should be in `.vscode/extensions.json` as a recommended extension instead. Low priority. | NEW |
| 018 | INFO | `.ai-utils/implementation-prompt.md` | AI prompt file tracked in git (63 lines). References `docs/ai-context-rules.md`, `docs/mvp-business-rules.md`, `docs/mvp-technical-specs.md`, and `tasks/@task-manager.md`. This is an intentional project artifact for AI-assisted development. No hygiene issue. | None — intentional. Verify referenced docs exist (covered by T2 audit). | NEW |

## Pre-Investigation Verification

| Pre-Inv # | Status | Notes |
|-----------|--------|-------|
| H1 | **CONFIRMED** | `.DS_Store` is tracked (`git ls-files .DS_Store` returns it). `.gitignore:12` has the pattern but file was added in commit 75715f2. Pattern only blocks new staging, not already-tracked files. |
| H2 | **CONFIRMED** | `.poc-frontend/` has 3 tracked files: 2 PNGs (6,844,051 + 6,068,725 = 12,912,776 bytes) and 1 HTML (58,246 bytes). PNGs are **not referenced** by `index.html` or any other file in the repo. Total bloat: ~12.3 MB. |
| H3 | **CONFIRMED** | `app/tsconfig.app.tsbuildinfo` is tracked. No `*.tsbuildinfo` pattern in `.gitignore`. |
| H4 | **CONFIRMED** | `app/tsconfig.node.tsbuildinfo` is tracked. Same missing gitignore pattern. |
| H5 | **CONFIRMED + EXPANDED** | `infra/deskai_infra.egg-info/` has 5 tracked files. Additionally discovered `backend/src/deskai_backend.egg-info/` with 5 more tracked files (finding #006). No `*.egg-info/` pattern in `.gitignore`. |
| H6 | **CONFIRMED** | `app-ci.yml` has no test step. Verified `app/package.json` has zero test dependencies — no vitest, jest, or mocha. No `test` script exists. |
| H7 | **CONFIRMED** | `infra-ci.yml:4` `pull_request:` trigger has no `paths:` filter. It fires on every PR. The `dorny/paths-filter` in the `detect-changes` job partially mitigates by gating the `infra` job, but a runner still spins up for every PR. |
| H8 | **CONFIRMED** | `deploy.yml:19` runs only `echo "Deployment pipeline placeholder. Implement in Task 004+."`. `workflow_dispatch` trigger means manual execution succeeds silently with exit code 0. |
| H9 | **CONFIRMED** | `CODEOWNERS:2` has a single `*` rule assigning 4 users to all paths. No path-specific ownership for `backend/`, `infra/`, `app/`, `contracts/`, or workflows. |
| H10 | **CONFIRMED + EXPANDED** | `.gitignore` is missing `*.egg-info/`, `*.tsbuildinfo`, `.poc-frontend/`, `*.egg`, `*.whl`, `.jsii-cache`. Currently only covers Python bytecode, node_modules, dist, venv, and env files. |
| H11 | **CONFIRMED** | `.prettierignore:9` has `.poc-frontend` but `.gitignore` does not. Inconsistency suggests intent to ignore was never completed. |
| H12 | **CONFIRMED** | Root `Makefile:1` declares `.PHONY: lint format format-check backend-test build-lambda infra-synth` — no `app-test` target. |

## Summary Statistics

- **Total findings**: 18
- **HIGH**: 2 (tracked `.DS_Store`, unreferenced 12 MB PNGs in git)
- **MEDIUM**: 6 (2 tsbuildinfo, 2 egg-info dirs, no app tests in CI, no contract validation in CI, no security scanning)
- **LOW**: 6 (deploy placeholder, CODEOWNERS wildcard, gitignore gaps, prettierignore inconsistency, missing Makefile target, infra-ci path filter)
- **INFO**: 2 (vscode Snyk setting, ai-utils prompt file)
- **Pre-investigation accuracy**: 12/12 confirmed, 2 expanded with additional findings, 4 new findings discovered
