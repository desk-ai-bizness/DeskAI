name: Deploy

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to deploy (dev or prod)'
        required: true
        type: choice
        options:
          - dev
          - prod

jobs:
  deploy-placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Not implemented
        run: |
          echo "ERROR: Deployment pipeline is not yet implemented. See Task 004+."
          exit 1
