# Runtime dependencies for Lambda handlers.
# Keep in sync with backend/pyproject.toml [project.dependencies].
# boto3 is provided by the Lambda runtime and intentionally excluded.
anthropic>=0.39.0
aws-lambda-powertools>=2.40.0
google-genai>=1.0.0
httpx>=0.27.0
pydantic>=2.13.3
python-jose[cryptography]>=3.3.0
tenacity>=9.0.0
